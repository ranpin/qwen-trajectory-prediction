# Deployment endpoint config. Source this before running eval/demo scripts:
#   set -a && . configs/deploy.env && set +a
# Or export ORIN_API_URL in your shell. Overridable per-command via --api_url.
# SSH target for deploy (user@host). The ssh-config alias is "orin-dog(勾)"
# which won't resolve unquoted, so use user@ip directly.
ORIN_HOST=vision@30.245.40.99
ORIN_SSH_USER=vision
ORIN_API_URL=http://30.245.40.99:8080
ORIN_MODEL=qwen3-4b-q4_k_m.gguf
